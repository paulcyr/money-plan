import React, {Component} from 'react';

class Strings extends Component {

	constructor() {
		super();
		let userLang = localStorage.getItem(key) || navigator.language || navigator.userLanguage;

	}
}